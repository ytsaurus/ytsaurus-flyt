[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wordcount-example"
version = "0.0.1"
description = "Minimal ytsaurus-flyt PyFlink example"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = ["pipeline"]
